name = "fh-hairbraiding"
compatibility_date = "2024-06-01"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "./dist"

# D1 Database binding — variable must be named exactly "DB"
[[d1_databases]]
binding = "DB"
database_name = "fh-bookings-db"
database_id = "22c8c657-a00e-4df4-a8e0-83f0c9c6d8b3"

# R2 Bucket binding
[[r2_buckets]]
binding = "BRAIDS_BUCKET"
bucket_name = "fh-braids-images"

# Plain vars (secrets like ADMIN_PASSWORD are set in Dashboard)
[vars]
STRIPE_SECRET_KEY = "sk_test_..."
STRIPE_WEBHOOK_SECRET = "whsec_..."
FRONTEND_URL = "https://fh-hairbraiding.pages.dev"
